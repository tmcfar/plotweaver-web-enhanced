# Development environment variables
NEXT_PUBLIC_API_URL=http://localhost:5000

# BFF (Backend-for-Frontend) for git-native operations
NEXT_PUBLIC_BFF_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
NEXT_PUBLIC_GIT_ENABLED=true

# To run with a real backend:
# 1. Start your Python backend on port 5000
# 2. Start BFF on port 8000 for git operations
# 3. Ensure CORS is configured to allow http://localhost:3000
# 
# The app will use mock data if the backend is unavailable
